test_cases:
  - id: TC001
    logical_scenario: lane_following
    keywords: [lane_detection, localization, path_planning, steering_control]
    seed_parameters:
      road_curvature: low
      ego_speed_kmh: 30

  - id: TC002
    logical_scenario: lane_following
    keywords: [lane_detection, localization, path_planning, steering_control]
    seed_parameters:
      road_curvature: medium
      ego_speed_kmh: 55

  - id: TC003
    logical_scenario: pedestrian_crossing
    keywords: [object_detection, lane_detection, path_planning, braking]
    seed_parameters:
      ego_speed_kmh: 30
      pedestrian_speed_ms: 1.2
      crossing_distance_m: 12
      occlusion: false

  - id: TC004
    logical_scenario: pedestrian_crossing
    keywords: [object_detection, lane_detection, path_planning, braking]
    seed_parameters:
      ego_speed_kmh: 45
      pedestrian_speed_ms: 1.8
      crossing_distance_m: 8
      occlusion: true

  - id: TC005
    logical_scenario: static_obstacle
    keywords: [object_detection, lane_detection, path_planning, braking]
    seed_parameters:
      ego_speed_kmh: 40
      obstacle_distance_m: 20
      obstacle_width_m: 1.0

  - id: TC006
    logical_scenario: static_obstacle
    keywords: [object_detection, lane_detection, path_planning, braking]
    seed_parameters:
      ego_speed_kmh: 65
      obstacle_distance_m: 12
      obstacle_width_m: 2.0

  - id: TC007
    logical_scenario: merge_into_traffic
    keywords: [object_detection, lane_detection, localization, path_planning, steering_control, braking]
    seed_parameters:
      ego_speed_kmh: 50
      target_lane_gap_m: 15
      other_vehicle_speed_kmh: 60

  - id: TC008
    logical_scenario: merge_into_traffic
    keywords: [object_detection, lane_detection, localization, path_planning, steering_control, braking]
    seed_parameters:
      ego_speed_kmh: 70
      target_lane_gap_m: 8
      other_vehicle_speed_kmh: 85

  - id: TC009
    logical_scenario: intersection_approach
    keywords: [object_detection, lane_detection, localization, path_planning, braking]
    seed_parameters:
      ego_speed_kmh: 35
      crossing_vehicle_speed_kmh: 30
      time_to_intersection_s: 5

  - id: TC010
    logical_scenario: intersection_approach
    keywords: [object_detection, lane_detection, localization, path_planning, braking]
    seed_parameters:
      ego_speed_kmh: 55
      crossing_vehicle_speed_kmh: 50
      time_to_intersection_s: 2
